---
phase: 01-board-und-digitale-uhr
plan: 01
type: execute
wave: 1
depends_on: []
files_modified: ["alarm-clock.yaml", "secrets.yaml"]
autonomous: false
requirements: ["TIME-01", "DISP-01", "DISP-03"]
must_haves:
  truths: ["ESPHome YAML kompiliert fehlerfrei mit ESP-IDF Framework und PSRAM", "NTP-Zeitsynchronisation ist konfiguriert mit Zeitzone Europe/Berlin", "Display-Treiber mipi_rgb mit Board-Modell T-RGB-2.1 ist konfiguriert", "Backlight ist als steuerbare Light-Entity konfiguriert", "Touchscreen cst816 ist konfiguriert", "LVGL zeigt digitale Uhrzeit und Datum mit Wochentag auf dem Display"]
  artifacts: [{"path": "alarm-clock.yaml", "provides": "Komplette ESPHome-Konfiguration fuer Phase 1", "contains": "framework:"}, {"path": "secrets.yaml", "provides": "WiFi-Credentials und API-Keys", "contains": "wifi_ssid"}]
  key_links: [{"from": "alarm-clock.yaml (lvgl)", "to": "alarm-clock.yaml (time)", "via": "LVGL time_format referenziert sntp_time", "pattern": "time: sntp_time"}, {"from": "alarm-clock.yaml (display)", "to": "alarm-clock.yaml (xl9535)", "via": "Display Reset/Enable Pins laufen ueber XL9535", "pattern": "xl9535: xl9535_hub"}]
---

<objective>
Komplette ESPHome-Konfiguration fuer das LilyGo T-RGB Board erstellen: Board-Setup (ESP-IDF, PSRAM, XL9535), Display (mipi_rgb), Touchscreen (cst816), Backlight (LEDC), NTP-Zeitsync, und LVGL-UI mit digitaler Uhrzeit + Datum/Wochentag.

Purpose: Board-Validierung -- nach diesem Plan kompiliert das Projekt und zeigt bei Flash auf die Hardware die aktuelle Uhrzeit mit Datum auf dem runden Display.
Output: alarm-clock.yaml (kompilierfaehig), secrets.yaml (Platzhalter)
</objective>

<execution_context>
@C:/Users/Daniel/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/Daniel/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-board-und-digitale-uhr/01-RESEARCH.md
@.planning/research/STACK.md
@.planning/research/PITFALLS.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: secrets.yaml mit Platzhaltern erstellen</name>
  <files>secrets.yaml</files>
  <read_first>
    - .planning/phases/01-board-und-digitale-uhr/01-RESEARCH.md
  </read_first>
  <action>
Datei `secrets.yaml` im Projekt-Root erstellen mit folgenden Platzhaltern:

```yaml
wifi_ssid: "DEIN_WLAN_NAME"
wifi_password: "DEIN_WLAN_PASSWORT"
api_key: "GENERIERTER_API_KEY"
ota_password: "DEIN_OTA_PASSWORT"
fallback_password: "FALLBACK_AP_PASSWORT"
```

Den API-Key mit einem Base64-String generieren (32 Bytes random). Per Python generieren:
`python -c "import secrets, base64; print(base64.b64encode(secrets.token_bytes(32)).decode())"`

Die generierten Werte als Platzhalter eintragen. Der Nutzer muss wifi_ssid und wifi_password vor dem ersten Flash anpassen.
  </action>
  <verify>
    <automated>test -f secrets.yaml && grep -q "wifi_ssid" secrets.yaml && grep -q "api_key" secrets.yaml && grep -q "ota_password" secrets.yaml && echo "PASS" || echo "FAIL"</automated>
  </verify>
  <acceptance_criteria>
    - secrets.yaml existiert im Projekt-Root
    - secrets.yaml enthaelt Key "wifi_ssid"
    - secrets.yaml enthaelt Key "wifi_password"
    - secrets.yaml enthaelt Key "api_key"
    - secrets.yaml enthaelt Key "ota_password"
    - secrets.yaml enthaelt Key "fallback_password"
  </acceptance_criteria>
  <done>secrets.yaml existiert mit allen 5 Keys (wifi_ssid, wifi_password, api_key, ota_password, fallback_password)</done>
</task>

<task type="auto">
  <name>Task 2: alarm-clock.yaml mit Board-Setup, Display, NTP und LVGL-Uhr erstellen</name>
  <files>alarm-clock.yaml</files>
  <read_first>
    - secrets.yaml
    - .planning/phases/01-board-und-digitale-uhr/01-RESEARCH.md
    - .planning/research/STACK.md
    - .planning/research/PITFALLS.md
  </read_first>
  <action>
Datei `alarm-clock.yaml` im Projekt-Root erstellen. Die Datei enthaelt die komplette Phase-1-Konfiguration in einer einzigen Datei (Package-Aufteilung erst ab Phase 2).

Die YAML-Datei muss folgende Sektionen in dieser Reihenfolge enthalten:

**1. ESPHome Core:**
```yaml
esphome:
  name: alarm-clock
  friendly_name: "Wecker"
  platformio_options:
    board_build.flash_mode: dio
```

**2. ESP32-S3 mit ESP-IDF (PFLICHT -- kein Arduino!):**
```yaml
esp32:
  board: esp32-s3-devkitc-1
  variant: esp32s3
  flash_size: 16MB
  framework:
    type: esp-idf
```

**3. PSRAM (PFLICHT fuer Display-Framebuffer):**
```yaml
psram:
  mode: octal
  speed: 80MHz
```

**4. Logger, API, OTA, WiFi:**
```yaml
logger:

api:
  encryption:
    key: !secret api_key

ota:
  - platform: esphome
    password: !secret ota_password

wifi:
  ssid: !secret wifi_ssid
  password: !secret wifi_password
  ap:
    ssid: "Wecker-Fallback"
    password: !secret fallback_password
```

**5. I2C Bus (GPIO8 SDA, GPIO48 SCL):**
```yaml
i2c:
  sda: GPIO8
  scl: GPIO48
```

**6. XL9535 I/O-Expander (MUSS vor Display stehen):**
```yaml
xl9535:
  - id: xl9535_hub
    address: 0x20
```

**7. Display (mipi_rgb mit T-RGB-2.1 Board-Modell):**
```yaml
display:
  - platform: mipi_rgb
    model: T-RGB-2.1
    id: my_display
    auto_clear_enabled: false
    update_interval: never
    reset_pin:
      xl9535: xl9535_hub
      number: 6
      inverted: true
    enable_pin:
      xl9535: xl9535_hub
      number: 2
      inverted: true
```

**8. Touchscreen (CST816 fuer T-RGB 2.1" Full Circle):**
```yaml
touchscreen:
  - platform: cst816
    id: my_touchscreen
    interrupt_pin: GPIO1
    reset_pin:
      xl9535: xl9535_hub
      number: 1
```

**9. Backlight (LEDC PWM auf GPIO46):**
```yaml
output:
  - platform: ledc
    id: backlight_output
    pin: GPIO46
    frequency: 1000Hz

light:
  - platform: monochromatic
    output: backlight_output
    name: "Display Backlight"
    id: backlight
    default_transition_length: 0.5s
    restore_mode: ALWAYS_ON
```

**10. NTP-Zeitsynchronisation (Europe/Berlin, deutsche NTP-Server):**
```yaml
time:
  - platform: sntp
    id: sntp_time
    timezone: "Europe/Berlin"
    servers:
      - 0.de.pool.ntp.org
      - 1.de.pool.ntp.org
      - 2.de.pool.ntp.org
    on_time_sync:
      then:
        - logger.log: "NTP-Zeit synchronisiert"
```

**11. LVGL mit digitaler Uhrzeitanzeige und Datum/Wochentag:**
```yaml
lvgl:
  displays:
    - my_display
  touchscreens:
    - my_touchscreen
  buffer_size: 25%
  pages:
    - id: digital_clock_page
      widgets:
        - label:
            id: time_label
            align: CENTER
            text_font: montserrat_48
            text_color: 0xFFFFFF
            text:
              time_format: "%H:%M"
              time: sntp_time
        - label:
            id: date_label
            align: CENTER
            y: 40
            text_font: montserrat_14
            text_color: 0xCCCCCC
            text:
              time_format: "%A, %d.%m.%Y"
              time: sntp_time
```

WICHTIG -- diese Fehler vermeiden:
- KEIN Arduino-Framework (nur esp-idf)
- KEIN QIO Flash-Mode (nur dio)
- PSRAM NICHT vergessen (mode: octal, speed: 80MHz)
- XL9535 MUSS vor dem Display-Block stehen
- auto_clear_enabled: false und update_interval: never PFLICHT fuer LVGL
- timezone als TZ-Datenbank-Name "Europe/Berlin" (NICHT als POSIX-String)
- LVGL buffer_size: 25% (nicht 100%, stabiler Start)
  </action>
  <verify>
    <automated>grep -q "type: esp-idf" alarm-clock.yaml && grep -q "mode: octal" alarm-clock.yaml && grep -q "model: T-RGB-2.1" alarm-clock.yaml && grep -q "xl9535_hub" alarm-clock.yaml && grep -q "time_format" alarm-clock.yaml && grep -q "Europe/Berlin" alarm-clock.yaml && grep -q "montserrat_48" alarm-clock.yaml && echo "PASS" || echo "FAIL"</automated>
  </verify>
  <acceptance_criteria>
    - alarm-clock.yaml enthaelt "type: esp-idf"
    - alarm-clock.yaml enthaelt "mode: octal" (PSRAM)
    - alarm-clock.yaml enthaelt "board_build.flash_mode: dio"
    - alarm-clock.yaml enthaelt "model: T-RGB-2.1" (mipi_rgb Display)
    - alarm-clock.yaml enthaelt "xl9535_hub" (I/O-Expander)
    - alarm-clock.yaml enthaelt "platform: cst816" (Touchscreen)
    - alarm-clock.yaml enthaelt "pin: GPIO46" (Backlight)
    - alarm-clock.yaml enthaelt 'timezone: "Europe/Berlin"' (NTP)
    - alarm-clock.yaml enthaelt "time_format" (LVGL Labels)
    - alarm-clock.yaml enthaelt "montserrat_48" (Uhrzeit-Font)
    - alarm-clock.yaml enthaelt "%H:%M" (Uhrzeit-Format)
    - alarm-clock.yaml enthaelt "%A, %d.%m.%Y" (Datum-Format mit Wochentag)
    - XL9535-Block steht VOR dem Display-Block in der Datei
  </acceptance_criteria>
  <done>alarm-clock.yaml existiert mit korrekter Board-Konfiguration (ESP-IDF, PSRAM, DIO), Display (mipi_rgb T-RGB-2.1 mit XL9535), Touchscreen (cst816), Backlight (LEDC GPIO46), NTP (Europe/Berlin), und LVGL-Page mit Uhrzeit-Label (HH:MM, montserrat_48) und Datum-Label (Wochentag + Datum, montserrat_14)</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Board-Flash und visuelle Pruefung</name>
  <files>alarm-clock.yaml, secrets.yaml</files>
  <read_first>
    - alarm-clock.yaml
    - secrets.yaml
  </read_first>
  <action>
Nutzer flasht das Board und prueft visuell ob Display, Uhrzeit und Datum korrekt angezeigt werden.
  </action>
  <what-built>
    Komplette ESPHome-Konfiguration fuer LilyGo T-RGB mit digitaler Uhrzeitanzeige.
    Board-Setup: ESP-IDF Framework, PSRAM octal, XL9535 I/O-Expander, mipi_rgb Display T-RGB-2.1, CST816 Touchscreen, LEDC Backlight auf GPIO46, SNTP mit Europe/Berlin.
    LVGL-UI: Digitale Uhrzeit (HH:MM, gross in der Mitte) und Datum mit Wochentag darunter.
  </what-built>
  <how-to-verify>
    1. secrets.yaml anpassen: wifi_ssid und wifi_password mit echten WLAN-Daten fuellen
    2. `esphome compile alarm-clock.yaml` ausfuehren -- muss ohne Fehler durchlaufen
    3. `esphome upload alarm-clock.yaml` ausfuehren -- Board flashen (USB oder OTA)
    4. Pruefen ob:
       - Display leuchtet (Backlight an)
       - Uhrzeit wird in grosser Schrift angezeigt (Format HH:MM)
       - Datum und Wochentag werden darunter angezeigt
       - Uhrzeit stimmt (NTP-Sync, Zeitzone Europe/Berlin)
    5. Im Serial-Log pruefen: "NTP-Zeit synchronisiert" Meldung erscheint
  </how-to-verify>
  <verify>
    <automated>esphome compile alarm-clock.yaml 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - esphome compile alarm-clock.yaml laeuft ohne Fehler durch
    - Display zeigt Uhrzeit im Format HH:MM
    - Datum und Wochentag sind sichtbar
    - Backlight leuchtet
  </acceptance_criteria>
  <done>Board zeigt aktuelle Uhrzeit und Datum auf dem runden Display, NTP-Sync bestaetigt</done>
  <resume-signal>Type "approved" oder beschreibe Probleme</resume-signal>
</task>

</tasks>

<verification>
- `esphome compile alarm-clock.yaml` kompiliert ohne Fehler
- Board zeigt aktuelle Uhrzeit (NTP-synchronisiert, Europe/Berlin) auf dem runden Display
- Datum und Wochentag sind unter der Uhrzeit sichtbar
- Display-Backlight ist eingeschaltet
- Serial-Log zeigt "NTP-Zeit synchronisiert"
</verification>

<success_criteria>
- alarm-clock.yaml und secrets.yaml existieren
- ESPHome kompiliert fehlerfrei mit ESP-IDF Framework
- Uhrzeit (HH:MM) wird auf dem Display angezeigt (TIME-01, DISP-01)
- Datum und Wochentag werden auf dem Display angezeigt (DISP-03)
- NTP-Sync funktioniert mit Zeitzone Europe/Berlin (TIME-01)
- Display-Backlight leuchtet
</success_criteria>

<output>
After completion, create `.planning/phases/01-board-und-digitale-uhr/01-01-SUMMARY.md`
</output>
