---
phase: 04-analoge-uhr-und-seitenwechsel
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - alarm-clock.yaml
autonomous: false
requirements:
  - DISP-02
  - DISP-06

must_haves:
  truths:
    - "Nutzer sieht eine analoge Uhr mit Stunden- und Minutenzeiger auf dem Display"
    - "Nutzer kann per HA-Select zwischen Digital und Analog wechseln"
    - "Beide Zifferblaetter zeigen die korrekte NTP-synchronisierte Uhrzeit"
    - "Nutzer kann die Hintergrundfarbe der Analog-Seite ueber HA aendern"
  artifacts:
    - path: "alarm-clock.yaml"
      provides: "Analog-Clock-Page mit Meter-Widget, Select-Entities, Update-Script"
      contains: "analog_clock_page"
    - path: "alarm-clock.yaml"
      provides: "Zifferblatt-Select Entity"
      contains: "clock_face_select"
    - path: "alarm-clock.yaml"
      provides: "Hintergrundfarb-Select Entity"
      contains: "bg_color_select"
  key_links:
    - from: "select: clock_face_select"
      to: "lvgl.page.show: analog_clock_page"
      via: "set_action mit if/else"
      pattern: "lvgl\\.page\\.show.*analog_clock_page"
    - from: "script: analog_time_update"
      to: "lvgl.indicator.update: minute_hand + hour_hand"
      via: "sntp_time Lambda"
      pattern: "lvgl\\.indicator\\.update"
    - from: "time: on_time"
      to: "script: analog_time_update"
      via: "lvgl.page.is_showing Bedingung"
      pattern: "analog_time_update"
---

<objective>
Analoge Uhren-Seite mit LVGL Meter-Widget und HA-gesteuerten Seitenwechsel zur bestehenden alarm-clock.yaml hinzufuegen.

Purpose: DISP-02 (Analoguhr mit Zeigern) und DISP-06 (Seitenwechsel Digital/Analog) abschliessen -- die letzten beiden Display-Requirements des Projekts.
Output: Erweiterte alarm-clock.yaml mit analog_clock_page, zwei neuen Select-Entities (Zifferblatt + Hintergrundfarbe), und minutenweisem Zeiger-Update-Script.
</objective>

<execution_context>
@C:/Users/Daniel/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/Daniel/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-analoge-uhr-und-seitenwechsel/04-CONTEXT.md
@.planning/phases/04-analoge-uhr-und-seitenwechsel/04-RESEARCH.md

<interfaces>
<!-- Bestehende Struktur in alarm-clock.yaml die der Executor kennen muss -->

Bestehende globals (alarm-clock.yaml Z.154-175):
- alarm_hour (int, restore_value)
- alarm_minute (int, restore_value)
- alarm_enabled (bool, restore_value)
- alarm_ringing (bool)
- alarm_melody_name (std::string, restore_value)

Bestehende select (alarm-clock.yaml Z.177-191):
- alarm_melody_select (template, options: Reveille/Entertainer/Frere Jacques)

Bestehende scripts (alarm-clock.yaml Z.229-265):
- alarm_timeout_script
- snooze_action
- alarm_stop_action

Bestehende LVGL pages (alarm-clock.yaml Z.303-581):
- digital_clock_page (Z.312)
- alarm_settings_page (Z.361)
- alarm_ringing_page (Z.525)

Bestehender time on_time trigger (alarm-clock.yaml Z.277-301):
- seconds: 0, minutes: /1 -- minutenweise Alarm-Pruefung

Bestehende Farben:
- Pink bg: 0xFF69B4
- Dark buttons: 0x1A1A2E
- Text weiss: 0xFFFFFF
- Text grau: 0xCCCCCC
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Analog-Uhr-Seite mit Meter-Widget, Globals und Update-Script</name>
  <files>alarm-clock.yaml</files>
  <read_first>
    - alarm-clock.yaml (vollstaendige aktuelle Config -- alle Abschnitte globals, scripts, time, lvgl)
    - .planning/phases/04-analoge-uhr-und-seitenwechsel/04-RESEARCH.md (Code-Beispiele fuer Meter-Widget und Script)
    - .planning/phases/04-analoge-uhr-und-seitenwechsel/04-CONTEXT.md (Locked Decisions: kein Sekundenzeiger, schwarze Zeiger, Pink-Standard)
  </read_first>
  <action>
Drei Abschnitte in alarm-clock.yaml ergaenzen:

**1. Global hinzufuegen (nach bestehenden globals, nach Zeile ~175):**
```yaml
  - id: bg_color_hex
    type: int
    restore_value: yes
    initial_value: '0xFF69B4'
```

**2. Script hinzufuegen (nach bestehenden scripts, nach alarm_stop_action):**
```yaml
  - id: analog_time_update
    then:
      - lvgl.indicator.update:
          id: minute_hand
          value: !lambda 'return id(sntp_time).now().minute;'
      - lvgl.indicator.update:
          id: hour_hand
          value: !lambda |-
            auto now = id(sntp_time).now();
            return std::fmod(now.hour, 12) * 60 + now.minute;
```

**3. Neue LVGL-Page hinzufuegen (nach alarm_ringing_page, am Ende des pages-Blocks):**
```yaml
    - id: analog_clock_page
      widgets:
        - obj:
            id: analog_clock_bg
            width: 480
            height: 480
            align: CENTER
            bg_color: 0xFF69B4
            border_width: 0
            pad_all: 0
            widgets:
              - meter:
                  id: analog_meter
                  height: 400
                  width: 400
                  align: CENTER
                  bg_opa: TRANSP
                  border_width: 0
                  text_color: 0x000000
                  scales:
                    - range_from: 0
                      range_to: 60
                      angle_range: 360
                      rotation: 270
                      ticks:
                        width: 1
                        count: 61
                        length: 10
                        color: 0x000000
                      indicators:
                        - line:
                            id: minute_hand
                            width: 3
                            color: 0x000000
                            r_mod: -4
                            value: 0
                    - range_from: 1
                      range_to: 12
                      angle_range: 330
                      rotation: 300
                      ticks:
                        width: 1
                        count: 12
                        length: 1
                        major:
                          stride: 1
                          width: 4
                          length: 10
                          color: 0x000000
                          label_gap: 14
                    - range_from: 0
                      range_to: 720
                      angle_range: 360
                      rotation: 270
                      ticks:
                        count: 0
                      indicators:
                        - line:
                            id: hour_hand
                            width: 5
                            color: 0x000000
                            r_mod: -50
                            value: 0
```

**4. Bestehenden on_time Block erweitern (im time: on_time: then: Block, NACH der bestehenden alarm-ringing Logik):**
Folgendes am Ende des bestehenden `then:` Blocks (nach der if-condition fuer alarm_ringing, ca. Zeile 301) hinzufuegen:
```yaml
          - if:
              condition:
                lvgl.page.is_showing: analog_clock_page
              then:
                - script.execute: analog_time_update
```

WICHTIG:
- Kein Sekundenzeiger (per User-Entscheidung)
- Zeiger-Farbe: 0x000000 (Schwarz, per User-Entscheidung)
- Hintergrundfarbe: 0xFF69B4 (Pink, per User-Entscheidung)
- Meter-Groesse: 400x400px auf dem 480x480px Display
- Exakt die Cookbook-Werte fuer angle_range/rotation verwenden (Pitfall 1+2 aus RESEARCH.md)
  </action>
  <verify>
    <automated>cd C:/Users/Daniel/dev/esp32-alarm-clock && grep -c "analog_clock_page" alarm-clock.yaml && grep -c "minute_hand" alarm-clock.yaml && grep -c "hour_hand" alarm-clock.yaml && grep -c "analog_time_update" alarm-clock.yaml && grep -c "bg_color_hex" alarm-clock.yaml</automated>
  </verify>
  <acceptance_criteria>
    - alarm-clock.yaml enthaelt `id: analog_clock_page`
    - alarm-clock.yaml enthaelt `id: analog_clock_bg` mit `bg_color: 0xFF69B4`
    - alarm-clock.yaml enthaelt `id: analog_meter` mit `height: 400` und `width: 400`
    - alarm-clock.yaml enthaelt `id: minute_hand` mit `color: 0x000000` und `width: 3`
    - alarm-clock.yaml enthaelt `id: hour_hand` mit `color: 0x000000` und `width: 5`
    - alarm-clock.yaml enthaelt `id: analog_time_update` Script mit `lvgl.indicator.update`
    - alarm-clock.yaml enthaelt `id: bg_color_hex` Global mit `initial_value: '0xFF69B4'`
    - on_time Block enthaelt `lvgl.page.is_showing: analog_clock_page` Bedingung
    - Stunden-Skala hat `range_to: 720`, `angle_range: 360`, `rotation: 270`
    - Labels-Skala hat `range_from: 1`, `range_to: 12`, `angle_range: 330`, `rotation: 300`
  </acceptance_criteria>
  <done>Analog-Uhr-Seite mit Meter-Widget (Stunden+Minutenzeiger), bg_color_hex Global, analog_time_update Script und minutenweiser on_time-Erweiterung in alarm-clock.yaml vorhanden</done>
</task>

<task type="auto">
  <name>Task 2: Select-Entities fuer Seitenwechsel und Hintergrundfarbe</name>
  <files>alarm-clock.yaml</files>
  <read_first>
    - alarm-clock.yaml (nach Task 1 aktualisierte Version -- select-Block und neue IDs pruefen)
    - .planning/phases/04-analoge-uhr-und-seitenwechsel/04-RESEARCH.md (Pattern 2+3: Select-Entity Beispiele)
    - .planning/phases/04-analoge-uhr-und-seitenwechsel/04-CONTEXT.md (Select-Optionen: Digital/Analog, Farben: Pink/Weiss/Schwarz/Dunkelblau)
  </read_first>
  <action>
Zwei neue Select-Entities zum bestehenden `select:` Block hinzufuegen (nach dem bestehenden `alarm_melody_select`):

**1. Zifferblatt-Select:**
```yaml
  - platform: template
    name: "Zifferblatt"
    id: clock_face_select
    icon: mdi:clock-outline
    options:
      - "Digital"
      - "Analog"
    initial_option: "Digital"
    optimistic: true
    set_action:
      - if:
          condition:
            lambda: 'return x == "Analog";'
          then:
            - script.execute: analog_time_update
            - lvgl.page.show: analog_clock_page
          else:
            - lvgl.page.show: digital_clock_page
```

**2. Hintergrundfarb-Select:**
```yaml
  - platform: template
    name: "Hintergrundfarbe"
    id: bg_color_select
    icon: mdi:palette
    options:
      - "Pink"
      - "Weiss"
      - "Schwarz"
      - "Dunkelblau"
    initial_option: "Pink"
    optimistic: true
    set_action:
      - lambda: |-
          if (x == "Pink") id(bg_color_hex) = 0xFF69B4;
          else if (x == "Weiss") id(bg_color_hex) = 0xFFFFFF;
          else if (x == "Schwarz") id(bg_color_hex) = 0x000000;
          else if (x == "Dunkelblau") id(bg_color_hex) = 0x1A1A2E;
      - lvgl.widget.update:
          id: analog_clock_bg
          bg_color: !lambda 'return lv_color_hex(id(bg_color_hex));'
```

WICHTIG:
- Seitenwechsel NUR ueber HA-Select (kein Touch-Trigger, per User-Entscheidung)
- `optimistic: true` verwenden (wie bestehendes Melodie-Select)
- `initial_option: "Digital"` damit nach Boot die Digital-Seite aktiv bleibt
- Bei Wechsel auf Analog: erst `analog_time_update` ausfuehren, dann Page zeigen (damit Zeiger sofort korrekt stehen)
- Farb-Select aendert NUR die Analog-Seite (nicht die Digital-Seite, per RESEARCH.md Empfehlung)
- Farb-Werte: Pink=0xFF69B4, Weiss=0xFFFFFF, Schwarz=0x000000, Dunkelblau=0x1A1A2E
  </action>
  <verify>
    <automated>cd C:/Users/Daniel/dev/esp32-alarm-clock && grep -c "clock_face_select" alarm-clock.yaml && grep -c "bg_color_select" alarm-clock.yaml && grep "mdi:clock-outline" alarm-clock.yaml && grep "mdi:palette" alarm-clock.yaml && grep -c "Dunkelblau" alarm-clock.yaml</automated>
  </verify>
  <acceptance_criteria>
    - alarm-clock.yaml enthaelt `id: clock_face_select` mit `options:` "Digital" und "Analog"
    - alarm-clock.yaml enthaelt `id: bg_color_select` mit `options:` "Pink", "Weiss", "Schwarz", "Dunkelblau"
    - clock_face_select hat `initial_option: "Digital"` und `optimistic: true`
    - clock_face_select set_action enthaelt `lvgl.page.show: analog_clock_page` und `lvgl.page.show: digital_clock_page`
    - clock_face_select set_action ruft `script.execute: analog_time_update` vor Page-Wechsel auf
    - bg_color_select set_action enthaelt Lambda mit `0xFF69B4`, `0xFFFFFF`, `0x000000`, `0x1A1A2E`
    - bg_color_select set_action enthaelt `lvgl.widget.update:` mit `id: analog_clock_bg`
    - bg_color_select hat `icon: mdi:palette`
    - clock_face_select hat `icon: mdi:clock-outline`
  </acceptance_criteria>
  <done>Zwei neue HA-Select-Entities (Zifferblatt + Hintergrundfarbe) in alarm-clock.yaml, Seitenwechsel funktioniert via set_action mit lvgl.page.show</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Compile-Check, OTA-Flash und visuelle Verifikation</name>
  <files>alarm-clock.yaml</files>
  <action>
Compile-Check und OTA-Flash ausfuehren, dann User zur visuellen Verifikation auffordern.
1. `esphome compile alarm-clock.yaml` ausfuehren -- muss ohne Fehler durchlaufen
2. `esphome upload alarm-clock.yaml --device [IP]` ausfuehren -- OTA-Flash auf das Board
3. User zur visuellen Pruefung der analogen Uhr und HA-Entities auffordern
  </action>
  <what-built>
    Analoge Uhren-Seite mit LVGL Meter-Widget (Stunden+Minutenzeiger), zwei HA-Select-Entities (Zifferblatt-Wechsel + Hintergrundfarbe), und minutenweiser Zeiger-Aktualisierung.
  </what-built>
  <how-to-verify>
    1. Compile-Check: `esphome compile alarm-clock.yaml` muss ohne Fehler durchlaufen
    2. OTA-Flash: `esphome upload alarm-clock.yaml --device [IP]` ausfuehren
    3. In Home Assistant pruefen:
       a. Neue Entity "Zifferblatt" (select) mit Optionen "Digital" / "Analog" sichtbar
       b. Neue Entity "Hintergrundfarbe" (select) mit Optionen "Pink" / "Weiss" / "Schwarz" / "Dunkelblau" sichtbar
    4. Zifferblatt-Select auf "Analog" stellen:
       a. Display zeigt analoge Uhr mit rundem Zifferblatt
       b. Stunden-Zahlen (1-12) sichtbar
       c. Minutenzeiger zeigt aktuelle Minute
       d. Stundenzeiger zeigt aktuelle Stunde (zwischen den Markierungen, nicht nur auf volle Stunde)
       e. Hintergrund ist Pink
    5. Zifferblatt-Select auf "Digital" stellen:
       a. Display wechselt zurueck zur digitalen Uhr
       b. Uhrzeit stimmt ueberein
    6. Hintergrundfarbe auf "Schwarz" stellen, dann auf "Analog" wechseln:
       a. Analog-Seite zeigt schwarzen Hintergrund
    7. Farbe auf "Weiss" stellen:
       a. Hintergrund wechselt sofort auf Weiss
  </how-to-verify>
  <verify>
    <automated>cd C:/Users/Daniel/dev/esp32-alarm-clock && esphome compile alarm-clock.yaml 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - esphome compile alarm-clock.yaml gibt keinen Fehler zurueck (exit code 0)
    - OTA-Flash erfolgreich auf das Board uebertragen
    - User bestaetigt: Analog-Uhr mit Stunden- und Minutenzeiger sichtbar
    - User bestaetigt: Seitenwechsel Digital/Analog via HA-Select funktioniert
    - User bestaetigt: Hintergrundfarb-Wechsel funktioniert
  </acceptance_criteria>
  <done>ESPHome kompiliert ohne Fehler, Board geflasht, User bestaetigt visuelle Korrektheit der analogen Uhr und beider Select-Entities</done>
  <resume-signal>Tippe "approved" oder beschreibe Probleme</resume-signal>
</task>

</tasks>

<verification>
- `esphome compile alarm-clock.yaml` kompiliert ohne Fehler
- HA zeigt 2 neue Select-Entities (Zifferblatt + Hintergrundfarbe)
- Analog-Seite zeigt Meter-Widget mit korrekten Zeiger-Positionen
- Seitenwechsel Digital/Analog funktioniert via HA-Select
- Farbwechsel der Analog-Seite funktioniert via HA-Select
</verification>

<success_criteria>
- DISP-02: Analoge Uhr mit Stunden- und Minutenzeiger sichtbar auf dem Display
- DISP-06: Seitenwechsel zwischen Digital und Analog via HA-Select funktional
- Beide Zifferblaetter zeigen NTP-synchronisierte korrekte Uhrzeit
- Hintergrundfarbe der Analog-Seite ueber HA aenderbar (4 Optionen)
- Bestehende Funktionalitaet (Alarm, Snooze, Melodie-Select) unveraendert
</success_criteria>

<output>
Nach Abschluss: `.planning/phases/04-analoge-uhr-und-seitenwechsel/04-01-SUMMARY.md` erstellen
</output>
