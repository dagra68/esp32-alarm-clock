---
phase: 02-alarm-kernfunktion
plan: 04
type: execute
wave: 1
depends_on: []
files_modified:
  - alarm-clock.yaml
autonomous: false
gap_closure: true
requirements:
  - ALRM-01
  - ALRM-02
  - ALRM-03
  - ALRM-04
  - ALRM-05
  - ALRM-06
  - DISP-04
  - DISP-05

must_haves:
  truths:
    - "Snooze-Button auf alarm_ringing_page reagiert physisch am Board (Melodie stoppt, Seite wechselt)"
    - "Stopp-Button auf alarm_ringing_page reagiert physisch am Board (Alarm dauerhaft deaktiviert)"
    - "alarm-clock.yaml enthaelt keinen buzzer_output LEDC-Block mehr"
    - "alarm-clock.yaml enthaelt i2s_audio-Block und speaker-Block"
    - "rtttl-Block referenziert speaker: alarm_speaker statt output: buzzer_output"
    - "Alarmzeit-Aenderung per Touch-Button wird sofort in HA-Entity alarm_time gespiegelt"
    - "weekday_only-Status ist auf dem Display (analog_alarm_label) sichtbar"
  artifacts:
    - path: "alarm-clock.yaml"
      provides: "I2S-Audio-Block mit i2s_audio + speaker: alarm_speaker"
      contains: "i2s_audio:"
    - path: "alarm-clock.yaml"
      provides: "rtttl ohne LEDC-Referenz"
      contains: "speaker: alarm_speaker"
    - path: "alarm-clock.yaml"
      provides: "alarm_ringing_page mit Touch-faehigem Hintergrund-Widget"
      contains: "alarm_ringing_bg"
    - path: "alarm-clock.yaml"
      provides: "component.update: alarm_time nach jedem +/- Button-Click"
      contains: "component.update: alarm_time"
    - path: "alarm-clock.yaml"
      provides: "weekday_only LVGL-Feedback in analog_alarm_label"
      contains: "Mo-Fr"
  key_links:
    - from: "alarm_ringing_page snooze_btn"
      to: "snooze_action Script"
      via: "on_short_click (Touch-Event empfangen durch bg-Widget)"
      pattern: "alarm_ringing_bg"
    - from: "hour_plus_btn / hour_minus_btn / minute_plus_btn / minute_minus_btn"
      to: "alarm_time HA-Entity"
      via: "component.update: alarm_time"
      pattern: "component\\.update: alarm_time"
    - from: "weekday_only_switch"
      to: "analog_alarm_label"
      via: "lvgl.label.update in turn_on/off_action"
      pattern: "lvgl\\.label\\.update.*analog_alarm_label"
---

<objective>
Gap-Fix-Plan: Alle 4 offenen Gaps aus Phase-2-Verifikation schliessen.

Purpose: Phase 2 (Alarm-Kernfunktion) in einen vollstaendig funktionierenden Zustand bringen,
damit Phase 3 (HA-Integration) auf einem stabilen Fundament aufbaut.

Output: alarm-clock.yaml mit (1) funktionierenden Touch-Buttons auf alarm_ringing_page,
(2) vollstaendiger I2S-Migration, (3) HA-gespiegelter Alarmzeit, (4) sichtbarem weekday_only-Status.
</objective>

<execution_context>
@C:/Users/Daniel/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/Daniel/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-alarm-kernfunktion/02-VERIFICATION.md
@.planning/phases/02-alarm-kernfunktion/02-03-SUMMARY.md

<interfaces>
<!-- Relevante Strukturen aus alarm-clock.yaml -- Executor muss Datei vor Aenderungen lesen -->

Zeilen 79-87 (output-Block -- buzzer_output wird ENTFERNT):
  output:
    - platform: ledc
      id: backlight_output
      pin: GPIO46
      frequency: 1000Hz
    - platform: ledc        # DIESER BLOCK WIRD ENTFERNT (Zeilen 84-87)
      id: buzzer_output
      pin: GPIO4
      frequency: 1000Hz

Zeilen 120-134 (weekday_only_switch -- turn_on/off_action wird ERWEITERT):
  turn_on_action:
    - globals.set:
        id: weekday_only
        value: 'true'
    # HIER: lvgl.label.update analog_alarm_label einfuegen
  turn_off_action:
    - globals.set:
        id: weekday_only
        value: 'false'
    # HIER: lvgl.label.update analog_alarm_label einfuegen

Zeilen 243-246 (rtttl-Block):
  rtttl:
    output: buzzer_output   # WIRD ERSETZT durch: speaker: alarm_speaker
    id: alarm_buzzer
    gain: 70%               # WIRD GEAENDERT zu: gain: 0.8

Zeilen 412-419 / 436-443 / 460-467 / 484-491 (4x on_short_click -- component.update fehlt):
  on_short_click:
    - lambda: 'id(alarm_hour) = (id(alarm_hour) + 1) % 24;'
    - lvgl.label.update:
        id: hour_value_label
        text: !lambda ...
    # HIER component.update: alarm_time ergaenzen

Zeilen 523-578 (alarm_ringing_page -- kein bg-Widget vorhanden):
  - id: alarm_ringing_page
    widgets:
      - label:
          id: ringing_header_label   # kein obj/bg-Widget davor
      - button:
          id: snooze_btn
      - button:
          id: stopp_btn

Zeilen 663-672 (analog_alarm_label -- weekday_only-Status fehlt):
  - label:
      id: analog_alarm_label
      text: !lambda |-
        static char buf[16];
        snprintf(buf, sizeof(buf), LV_SYMBOL_BELL " %02d:%02d",
                 id(alarm_hour), id(alarm_minute));
        return buf;
      # Kein weekday_only-Suffix
</interfaces>
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: alarm_ringing_page Touch-Fix (bg-Widget als Event-Target)</name>

  <read_first>
    - alarm-clock.yaml (vollstaendig lesen, Fokus Zeilen 523-578 alarm_ringing_page)
    - .planning/phases/02-alarm-kernfunktion/02-VERIFICATION.md (Gap 2 Beschreibung)
  </read_first>

  <files>alarm-clock.yaml</files>

  <action>
Die alarm_ringing_page (ab Zeile 523) hat keinen Hintergrund-Container-Widget. LVGL liefert
Touch-Events nur an Widgets, die als clickable markiert sind. Da kein bg-Widget vorhanden ist,
landen Clicks auf dem freien Seitenbereich ins Leere und snooze_btn / stopp_btn empfangen
moeglicherweise keine Events, weil die Touch-Koordinaten-Zuordnung fehlschlaegt.

FIX: Ein obj-Widget als vollflaeching Hintergrund-Container am Anfang der alarm_ringing_page-
Widget-Liste einfuegen. Dieses obj muss:
  - id: alarm_ringing_bg haben
  - width: 480, height: 480, align: CENTER
  - bg_color: 0x1A1A2E (dunkelblau, passend zum restlichen Design)
  - border_width: 0, pad_all: 0
  - clickable: false (nur als Hit-Flaeche fuer LVGL-Event-Routing, kein eigenes Click-Event)

Das obj muss als ERSTES Widget in der alarm_ringing_page-Widget-Liste stehen (vor dem
ringing_header_label). Dadurch registriert LVGL den Seitenbereich als valide Touch-Flaeche
und routet Events korrekt zu snooze_btn und stopp_btn.

Konkreter YAML-Block, der am Anfang der alarm_ringing_page widgets-Liste eingefuegt wird
(Einrueckung muss mit den anderen Widgets auf dieser Seite uebereinstimmen):

        - obj:
            id: alarm_ringing_bg
            width: 480
            height: 480
            align: CENTER
            bg_color: 0x1A1A2E
            border_width: 0
            pad_all: 0
            clickable: false

Die bestehenden Widgets (ringing_header_label, ringing_time_label, snooze_btn, stopp_btn)
bleiben unveraendert -- nur Reihenfolge und Einrueckung pruefen.

WICHTIG: Die Buttons snooze_btn und stopp_btn NICHT veraendern -- ihre on_short_click Handler
(script.execute: snooze_action / script.execute: alarm_stop_action) sind korrekt verdrahtet.
  </action>

  <verify>
    <automated>grep -n "alarm_ringing_bg" /c/Users/Daniel/dev/esp32-alarm-clock/alarm-clock.yaml</automated>
  </verify>

  <acceptance_criteria>
    - alarm-clock.yaml enthaelt eine Zeile mit "id: alarm_ringing_bg"
    - alarm-clock.yaml enthaelt "width: 480" und "height: 480" im Kontext des alarm_ringing_bg obj
    - alarm_ringing_bg steht VOR ringing_header_label in der Widget-Liste (niedrigere Zeilennummer)
    - snooze_btn on_short_click zeigt noch "script.execute: snooze_action" (unveraendert)
    - stopp_btn on_short_click zeigt noch "script.execute: alarm_stop_action" (unveraendert)
  </acceptance_criteria>

  <done>
alarm_ringing_page hat ein vollflaechiges bg-Widget (alarm_ringing_bg, 480x480) als erstes Widget.
LVGL kann Touch-Events auf der gesamten Seitenflaeche empfangen. snooze_btn und stopp_btn-Handler
sind unveraendert und korrekt verdrahtet.
  </done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: I2S-Migration abschliessen (LEDC entfernen, i2s_audio + speaker einfuegen)</name>

  <read_first>
    - alarm-clock.yaml (vollstaendig lesen, Fokus Zeilen 79-87 output-Block und Zeilen 243-246 rtttl-Block)
    - .planning/phases/02-alarm-kernfunktion/02-VERIFICATION.md (Gap 1 Beschreibung, Zeilen 29-41)
    - .planning/phases/02-alarm-kernfunktion/02-RESEARCH.md (Pattern 1: I2S Audio + RTTTL Speaker, Zeilen 145-179)
  </read_first>

  <files>alarm-clock.yaml</files>

  <action>
Drei Aenderungen in alarm-clock.yaml:

AENDERUNG 1: buzzer_output LEDC-Block entfernen (Zeilen 84-87)

Den gesamten zweiten output-Eintrag loeschen:
  - platform: ledc        # DIESE 4 ZEILEN ENTFERNEN
    id: buzzer_output
    pin: GPIO4
    frequency: 1000Hz

Der backlight_output-Block (Zeilen 80-83) bleibt unveraendert.

AENDERUNG 2: i2s_audio-Block und speaker-Block einfuegen

Nach dem output-Block (nach Zeile 87, vor dem light-Block) einfuegen:

i2s_audio:
  i2s_lrclk_pin: GPIO6
  i2s_bclk_pin: GPIO5

speaker:
  - platform: i2s_audio
    id: alarm_speaker
    dac_type: external
    i2s_dout_pin: GPIO7
    channel: mono
    sample_rate: 16000
    bits_per_sample: 16bit

HINWEIS zu den GPIO-Pins: GPIO5 (BCLK), GPIO6 (LRCLK/WS), GPIO7 (DOUT) sind die
Standard-I2S-Pins des LilyGo T-RGB ESP32-S3 Boards gemaess Pinout. Falls der Compiler
diese ablehnt oder Konflikte entstehen: in der Board-Dokumentation unter
/c/Users/Daniel/dev/esp32-alarm-clock/ nach einer utilities.h oder Schaltplan-Datei
suchen und die korrekten Pins verwenden. Die I2S-Migration ist ALRM-03-seitig notwendig
auch wenn die Hardware (MAX98357A) noch nicht angeschlossen ist.

AENDERUNG 3: rtttl-Block auf speaker umstellen (Zeile 243-246)

Zeile 244: "output: buzzer_output" ersetzen durch "speaker: alarm_speaker"
Zeile 246: "gain: 70%" ersetzen durch "gain: 0.8"

Resultat:
rtttl:
  speaker: alarm_speaker
  id: alarm_buzzer
  gain: 0.8
  on_finished_playback:
    # ... unveraendert

Nach den Aenderungen "esphome compile alarm-clock.yaml" ausfuehren um Kompilierbarkeit
zu pruefen. Bei Compile-Fehler wegen I2S-Pins: Pins anpassen (GPIO38, GPIO39, GPIO40
als Alternative pruefen) und erneut kompilieren bis Erfolg.
  </action>

  <verify>
    <automated>grep -n "buzzer_output\|i2s_audio\|alarm_speaker\|i2s_lrclk\|i2s_bclk\|i2s_dout" /c/Users/Daniel/dev/esp32-alarm-clock/alarm-clock.yaml</automated>
  </verify>

  <acceptance_criteria>
    - alarm-clock.yaml enthaelt KEINE Zeile mit "buzzer_output" (weder im output-Block noch in rtttl)
    - alarm-clock.yaml enthaelt "i2s_audio:" (eigene Zeile, kein Einzug)
    - alarm-clock.yaml enthaelt "i2s_lrclk_pin:" und "i2s_bclk_pin:"
    - alarm-clock.yaml enthaelt "id: alarm_speaker"
    - alarm-clock.yaml enthaelt "i2s_dout_pin:"
    - alarm-clock.yaml enthaelt "speaker: alarm_speaker" im rtttl-Block
    - alarm-clock.yaml enthaelt "gain: 0.8" im rtttl-Block (nicht mehr "gain: 70%")
    - "esphome compile alarm-clock.yaml" laeuft ohne Fehler durch
  </acceptance_criteria>

  <done>
I2S-Migration vollstaendig: kein LEDC buzzer_output mehr, i2s_audio-Block mit T-RGB-Pins vorhanden,
speaker: alarm_speaker konfiguriert, rtttl auf speaker umgestellt, Firmware kompiliert fehlerfrei.
ALRM-03 ist code-seitig implementiert (Hardware-Verdrahtung MAX98357A bleibt separater Schritt fuer Nutzer).
  </done>
</task>

<task type="auto" tdd="false">
  <name>Task 3: HA-Spiegel fuer Alarmzeit-Buttons + weekday_only Display-Feedback</name>

  <read_first>
    - alarm-clock.yaml (Zeilen 403-497 +/- Buttons, Zeilen 120-134 weekday_only_switch, Zeilen 148-173 alarm_time set_action, Zeilen 663-672 analog_alarm_label)
    - .planning/phases/02-alarm-kernfunktion/02-VERIFICATION.md (Gap 3 Zeilen 43-50, Gap 4 Zeilen 52-61)
  </read_first>

  <files>alarm-clock.yaml</files>

  <action>
Zwei Gruppen von Aenderungen in alarm-clock.yaml:

GRUPPE A: component.update: alarm_time nach jedem der 4 +/- Buttons einfuegen

Alle vier Buttons (hour_plus_btn, hour_minus_btn, minute_plus_btn, minute_minus_btn) haben
identische Struktur. Nach dem letzten lvgl.label.update-Schritt in jedem on_short_click
muss ein "- component.update: alarm_time" ergaenzt werden.

Fuer hour_plus_btn (on_short_click ab Zeile 412) -- Ziel-Zustand:
            on_short_click:
              - lambda: 'id(alarm_hour) = (id(alarm_hour) + 1) % 24;'
              - lvgl.label.update:
                  id: hour_value_label
                  text: !lambda |-
                    static char buf[4];
                    snprintf(buf, sizeof(buf), "%02d", id(alarm_hour));
                    return buf;
              - component.update: alarm_time    # NEU

Gleiches Muster fuer hour_minus_btn, minute_plus_btn, minute_minus_btn.
Das component.update: alarm_time triggert die lambda-Funktion der datetime-Entity,
die id(alarm_hour) und id(alarm_minute) liest -- HA-Entity zeigt sofort den neuen Wert.

GRUPPE B: weekday_only-Status in analog_alarm_label anzeigen

Teilaenderung B1: weekday_only_switch turn_on_action und turn_off_action erweitern (Zeilen 127-134).

Ziel-Zustand turn_on_action:
    turn_on_action:
      - globals.set:
          id: weekday_only
          value: 'true'
      - lvgl.label.update:
          id: analog_alarm_label
          text: !lambda |-
            static char buf[24];
            snprintf(buf, sizeof(buf), LV_SYMBOL_BELL " %02d:%02d Mo-Fr",
                     id(alarm_hour), id(alarm_minute));
            return buf;

Ziel-Zustand turn_off_action:
    turn_off_action:
      - globals.set:
          id: weekday_only
          value: 'false'
      - lvgl.label.update:
          id: analog_alarm_label
          text: !lambda |-
            static char buf[24];
            snprintf(buf, sizeof(buf), LV_SYMBOL_BELL " %02d:%02d",
                     id(alarm_hour), id(alarm_minute));
            return buf;

Teilaenderung B2: Initial-Text von analog_alarm_label aktualisieren (Zeilen 668-672),
damit der Suffix beim Booten korrekt angezeigt wird (weekday_only ist per Default 'true').

Ziel-Zustand analog_alarm_label text:
                        text: !lambda |-
                          static char buf[24];
                          if (id(weekday_only)) {
                            snprintf(buf, sizeof(buf), LV_SYMBOL_BELL " %02d:%02d Mo-Fr",
                                     id(alarm_hour), id(alarm_minute));
                          } else {
                            snprintf(buf, sizeof(buf), LV_SYMBOL_BELL " %02d:%02d",
                                     id(alarm_hour), id(alarm_minute));
                          }
                          return buf;

HINWEIS: buf-Groesse von 16 auf 24 erhoehen (LV_SYMBOL_BELL = 3 Bytes UTF-8 + " HH:MM Mo-Fr" = 13 Zeichen + Nullbyte = 17, 24 als sicherer Puffer).

Teilaenderung B3: alarm_time datetime set_action (Zeile 155-161) -- analog_alarm_label-Update
auf das neue Format mit weekday_only-Pruefung umstellen:
      - lvgl.label.update:
          id: analog_alarm_label
          text: !lambda |-
            static char buf[24];
            if (id(weekday_only)) {
              snprintf(buf, sizeof(buf), LV_SYMBOL_BELL " %02d:%02d Mo-Fr",
                       id(alarm_hour), id(alarm_minute));
            } else {
              snprintf(buf, sizeof(buf), LV_SYMBOL_BELL " %02d:%02d",
                       id(alarm_hour), id(alarm_minute));
            }
            return buf;

HINWEIS: alarm_enabled_switch Aktionen aendern nur text_color (nicht den Text) -- dieses Muster
unveraendert lassen. Kein Konflikt mit den weekday_only-Aenderungen.
  </action>

  <verify>
    <automated>grep -c "component.update: alarm_time" /c/Users/Daniel/dev/esp32-alarm-clock/alarm-clock.yaml</automated>
  </verify>

  <acceptance_criteria>
    - "grep -c 'component.update: alarm_time' alarm-clock.yaml" gibt 4 aus (eine pro +/- Button)
    - "grep -c 'Mo-Fr' alarm-clock.yaml" gibt mindestens 3 aus (turn_on_action, turn_off_action, analog_alarm_label initial text)
    - weekday_only_switch turn_on_action enthaelt "lvgl.label.update" mit "id: analog_alarm_label"
    - weekday_only_switch turn_off_action enthaelt "lvgl.label.update" mit "id: analog_alarm_label"
    - Alle snprintf-Aufrufe mit "Mo-Fr" im Puffer verwenden buf[24] (nicht buf[16])
    - "esphome compile alarm-clock.yaml" laeuft ohne Fehler durch
  </acceptance_criteria>

  <done>
Alarmzeit-Aenderungen per Touch werden sofort in HA gespiegelt (4x component.update: alarm_time).
weekday_only-Status ist auf dem Display sichtbar: analog_alarm_label zeigt "Mo-Fr" Suffix wenn
nur-Wochentag aktiv, kein Suffix wenn jeden-Tag aktiv. Beim Umschalten per HA-Switch wird
Label sofort aktualisiert.
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 4: OTA-Flash und Board-Verifikation aller Gap-Fixes</name>
  <files>alarm-clock.yaml</files>
  <action>
OTA-Flash der neuen Firmware ausfuehren:
  cd /c/Users/Daniel/dev/esp32-alarm-clock
  esphome upload alarm-clock.yaml
  </action>
  <verify>
    <automated>esphome compile /c/Users/Daniel/dev/esp32-alarm-clock/alarm-clock.yaml 2>&1 | tail -5</automated>
  </verify>
  <done>OTA-Flash erfolgreich abgeschlossen, Board bootet mit neuer Firmware.</done>

  <what-built>
Alle 4 Gaps aus Phase-2-Verifikation wurden adressiert:
1. alarm_ringing_page: bg-Widget (alarm_ringing_bg, 480x480) als erster Widget eingefuegt
2. I2S-Migration: buzzer_output entfernt, i2s_audio + speaker: alarm_speaker eingefuegt, rtttl umgestellt
3. HA-Spiegel: component.update: alarm_time nach jedem der 4 +/- Buttons ergaenzt
4. weekday_only Display: "Mo-Fr" Suffix in analog_alarm_label, turn_on/off_action aktualisiert

Firmware wurde kompiliert (esphome compile). OTA-Flash muss jetzt ausgefuehrt werden.
  </what-built>

  <how-to-verify>
1. OTA-Flash ausfuehren:
     cd /c/Users/Daniel/dev/esp32-alarm-clock
     esphome upload alarm-clock.yaml

2. Nach erfolgreichem Upload -- Alarmzeit-HA-Spiegel testen:
   - Home Assistant oeffnen -> Wecker-Device -> "Alarmzeit" Entity beobachten
   - Am Display: alarm_settings_page oeffnen (Long-Press auf Alarm-Button)
   - Stunde per + oder - Button aendern
   - ERWARTET: "Alarmzeit" Entity in HA zeigt sofort die neue Stunde (kein Reload noetig)

3. weekday_only Display-Feedback testen:
   - In HA: "Nur Mo-Fr" Switch ausschalten
   - ERWARTET: analog_alarm_label auf dem Display zeigt nur "Bell HH:MM" (kein "Mo-Fr" Suffix)
   - In HA: "Nur Mo-Fr" Switch einschalten
   - ERWARTET: analog_alarm_label zeigt "Bell HH:MM Mo-Fr"

4. Snooze/Stopp-Button Touch-Test (erfordert aktiven Alarm):
   - Alarmzeit auf naechste Minute stellen, Alarm aktivieren, warten bis Alarm klingelt
   - alarm_ringing_page erscheint
   - "Snooze 5min" antippen: ERWARTET: Melodie stoppt sofort, Seite wechselt zu analog_clock_page
   - Alarm erneut ausloesen, diesmal "Stopp" antippen: ERWARTET: Melodie stoppt, Seite wechselt,
     analog_alarm_label ist grau (Alarm inaktiv), Alarm klingelt NICHT erneut nach 5 Minuten

5. I2S-Audio (nur wenn MAX98357A physisch angeschlossen):
   - Alarm ausloesen
   - ERWARTET: Melodie hoerbar ueber Lautsprecher
   - Falls Lautsprecher NICHT angeschlossen: kein Ton erwartet -- das ist korrekt
  </how-to-verify>

  <resume-signal>
Tipp "approved" wenn alle testbaren Punkte PASS, oder beschreibe welche Tests fehlgeschlagen sind
(z.B. "Snooze reagiert immer noch nicht -- Stopp funktioniert"). Gap-Details helfen beim naechsten Fix.
  </resume-signal>
</task>

</tasks>

<verification>
Nach Ausfuehrung aller Tasks muss gelten:

1. "grep -c 'buzzer_output' alarm-clock.yaml" gibt 0 aus
2. "grep -n 'i2s_audio:' alarm-clock.yaml" zeigt genau 1 Treffer
3. "grep -n 'speaker: alarm_speaker' alarm-clock.yaml" zeigt genau 1 Treffer (im rtttl-Block)
4. "grep -n 'id: alarm_ringing_bg' alarm-clock.yaml" zeigt genau 1 Treffer
5. "grep -c 'component.update: alarm_time' alarm-clock.yaml" gibt 4 aus
6. "grep -c 'Mo-Fr' alarm-clock.yaml" gibt mindestens 3 aus
7. "esphome compile alarm-clock.yaml" endet ohne Fehler
</verification>

<success_criteria>
Phase 2 (Alarm-Kernfunktion) gilt als vollstaendig wenn nach OTA-Flash gilt:
- Snooze-Button auf alarm_ringing_page reagiert physisch (ALRM-04, ALRM-05)
- Stopp-Button auf alarm_ringing_page reagiert physisch (ALRM-04)
- Alarmzeit-Aenderung per Touch wird sofort in HA gespiegelt (ALRM-02)
- weekday_only-Status sichtbar auf Display (ALRM-06)
- I2S-Code ist korrekt (ALRM-03 code-seitig -- Hardware-Verdrahtung ist separater User-Schritt)
- Firmware kompiliert und laeuft stabil
</success_criteria>

<output>
Nach Abschluss und Checkpoint-Bestaetigung:
".planning/phases/02-alarm-kernfunktion/02-04-SUMMARY.md" erstellen mit:
- Welche Gaps geschlossen wurden
- OTA-Flash-Ergebnis
- Board-Verifikations-Ergebnis (Checkpoint-Antwort)
- Offene Punkte (z.B. I2S-Audio Hardware-Verdrahtung)
</output>
