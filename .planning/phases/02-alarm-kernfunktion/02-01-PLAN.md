---
phase: 02-alarm-kernfunktion
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - alarm-clock.yaml
autonomous: false
requirements:
  - ALRM-03
  - ALRM-05
must_haves:
  truths:
    - "alarm-clock.yaml kompiliert fehlerfrei nach der I2S-Migration"
    - "RTTTL-Komponente referenziert alarm_speaker (I2S) statt buzzer_output (LEDC)"
    - "GPIO0 binary_sensor (snooze_button) ist vollstaendig entfernt"
    - "LEDC-Output buzzer_output auf GPIO38 ist vollstaendig entfernt"
    - "I2S-Audio-Block mit korrekten T-RGB-Pin-Nummern ist in alarm-clock.yaml vorhanden"
  artifacts:
    - path: "alarm-clock.yaml"
      provides: "Vollstaendige ESPHome-Konfiguration mit I2S-Audio statt LEDC-Buzzer"
      contains: "i2s_audio"
  key_links:
    - from: "rtttl"
      to: "speaker: alarm_speaker"
      via: "ESPHome rtttl Komponente"
      pattern: "speaker: alarm_speaker"
    - from: "i2s_audio"
      to: "MAX98357A Hardware"
      via: "GPIO BCLK/LRCLK/DOUT Pins"
      pattern: "i2s_lrclk_pin:"
---

<objective>
I2S-Audio-Migration: LEDC PWM Buzzer auf GPIO38 durch I2S-Speaker mit MAX98357A ersetzen, GPIO0 Binary-Sensor (Snooze-Button) entfernen. Chirurgische Eingriffe in alarm-clock.yaml -- keine neuen Logik-Aenderungen.

Purpose: Der Alarm soll ueber einen echten Lautsprecher (MAX98357A Verstaerker-Modul) klingen statt ueber einen Piezo-Buzzer. GPIO0 ist ein Boot-Strapping-Pin und darf nicht als Input verwendet werden.

Output: alarm-clock.yaml mit i2s_audio + speaker-Block statt ledc output, rtttl referenziert alarm_speaker, GPIO0 binary_sensor entfernt.
</objective>

<execution_context>
@C:/Users/Daniel/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/Daniel/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/02-alarm-kernfunktion/02-CONTEXT.md
@.planning/phases/02-alarm-kernfunktion/02-RESEARCH.md

<interfaces>
<!-- Aktueller Zustand von alarm-clock.yaml (relevante Abschnitte) -->

Zu ENTFERNENDE Bloecke (exakter YAML-Text):

```yaml
# ENTFERNEN -- output buzzer_output (Zeilen ~84-88):
  - platform: ledc
    id: buzzer_output
    pin: GPIO38
    frequency: 1000Hz

# ENTFERNEN -- binary_sensor snooze_button (Zeilen ~240-257):
binary_sensor:
  - platform: gpio
    pin:
      number: GPIO0
      mode:
        input: true
        pullup: true
      inverted: true
      ignore_strapping_warning: true
    id: snooze_button
    name: "Snooze Button"
    internal: true
    on_press:
      - if:
          condition:
            lambda: 'return id(alarm_ringing);'
          then:
            - script.execute: snooze_action

# ENTFERNEN -- rtttl output-Referenz (Zeilen ~223-224):
rtttl:
  output: buzzer_output   # <-- Diese Zeile ersetzen durch: speaker: alarm_speaker
```

Zu ERGAENZENDE Bloecke (exakter YAML-Text nach der i2c-Sektion einzufuegen):

```yaml
i2s_audio:
  i2s_lrclk_pin: GPIOX   # T-RGB I2S WS/LRCLK -- aus Schaltplan/Pinout ermitteln
  i2s_bclk_pin:  GPIOX   # T-RGB I2S BCLK     -- aus Schaltplan/Pinout ermitteln

speaker:
  - platform: i2s_audio
    id: alarm_speaker
    dac_type: external
    i2s_dout_pin: GPIOX   # T-RGB I2S DOUT/DIN -- aus Schaltplan/Pinout ermitteln
    channel: mono
    sample_rate: 16000
    bits_per_sample: 16bit
```

Bekannte belegte GPIOs (darf NICHT fuer I2S verwendet werden):
GPIO1=Touch IRQ, GPIO2-18=RGB Display, GPIO21=RGB, GPIO38=SD DAT, GPIO39=SD CLK,
GPIO40=SD CMD, GPIO41=VSYNC, GPIO42=PCLK, GPIO43-45=RGB, GPIO46=Backlight,
GPIO47=HSYNC, GPIO48=I2C SCL, GPIO8=I2C SDA, GPIO4=SPI MOSI, GPIO5=SPI CLK

Potenziell freie GPIOs fuer I2S: GPIO19, GPIO20
(GPIO0 ist Boot-Strapping-Pin -- nicht verwenden)
</interfaces>
</context>

<tasks>

<task type="checkpoint:human-action">
  <name>Task 1: I2S-Pin-Nummern am T-RGB Board ermitteln</name>
  <files>alarm-clock.yaml</files>
  <action>
Ermittle die tatsaechlichen GPIO-Nummern fuer die drei I2S-Signale des MAX98357A-Moduls am T-RGB Board.

Option A (empfohlen): LilyGo T-RGB Schaltplan lesen
- URL: https://github.com/Xinyuan-LilyGO/LilyGo-T-RGB/blob/master/schematic/T-RGB.pdf
- Suche nach I2S-Pins: BCLK (Bit Clock), WS/LRCLK (Word Select), DOUT/DIN (Data)
- Notiere die drei GPIO-Nummern

Option B: MAX98357A Modul physisch verdrahten
- Schliesse das MAX98357A-Modul an drei freie GPIOs an
- Kandidaten: GPIO19 fuer BCLK, GPIO20 fuer LRCLK, GPIO22 fuer DOUT
- Notiere die gewaehlten GPIOs

Bekannte belegte GPIOs (darf NICHT verwendet werden):
GPIO0(Boot), GPIO1(Touch IRQ), GPIO2-18(RGB), GPIO21(RGB), GPIO38(SD DAT),
GPIO39(SD CLK), GPIO40(SD CMD), GPIO41(VSYNC), GPIO42(PCLK), GPIO43-45(RGB),
GPIO46(Backlight), GPIO47(HSYNC), GPIO48(I2C SCL), GPIO8(I2C SDA),
GPIO4(SPI MOSI), GPIO5(SPI CLK)
  </action>
  <verify>
    <automated>MISSING -- manueller Schritt, kein automatisierter Verify moeglich</automated>
  </verify>
  <acceptance_criteria>
    - Drei GPIO-Nummern sind bekannt: BCLK, LRCLK, DOUT
    - Alle drei GPIOs sind nicht in der Liste der belegten Pins
  </acceptance_criteria>
  <done>GPIO-Nummern fuer BCLK, LRCLK und DOUT sind ermittelt und bereit fuer Task 2.</done>
  <resume-signal>Teile die drei GPIO-Nummern mit: BCLK=GPIOxx, LRCLK=GPIOxx, DOUT=GPIOxx</resume-signal>
</task>

<task type="auto">
  <name>Task 2: LEDC-Buzzer entfernen und I2S-Audio-Block ergaenzen</name>
  <files>alarm-clock.yaml</files>
  <read_first>
    - alarm-clock.yaml (vollstaendig lesen um exakte Zeilennummern und Kontext zu kennen)
    - .planning/phases/02-alarm-kernfunktion/02-CONTEXT.md (Entscheidungen: I2S statt LEDC)
    - .planning/phases/02-alarm-kernfunktion/02-RESEARCH.md (Pattern 1: I2S Audio + RTTTL Speaker)
  </read_first>
  <action>
Fuehre folgende chirurgische Aenderungen in alarm-clock.yaml durch (GPIO-Nummern aus Task 1 verwenden):

ENTFERNEN -- output buzzer_output Block:
Loesche diese 4 Zeilen vollstaendig aus dem output:-Abschnitt:
```yaml
  - platform: ledc
    id: buzzer_output
    pin: GPIO38
    frequency: 1000Hz
```
Der output:-Abschnitt enthaelt danach nur noch backlight_output (LEDC auf GPIO46).

ERGAENZEN -- i2s_audio Block:
Fuege nach der i2c:-Sektion (nach `  scl: GPIO48`) einen neuen Top-Level-Block ein:
```yaml
i2s_audio:
  i2s_lrclk_pin: GPIO[LRCLK aus Task 1]
  i2s_bclk_pin:  GPIO[BCLK aus Task 1]

speaker:
  - platform: i2s_audio
    id: alarm_speaker
    dac_type: external
    i2s_dout_pin: GPIO[DOUT aus Task 1]
    channel: mono
    sample_rate: 16000
    bits_per_sample: 16bit
```

AENDERN -- rtttl Komponente:
Zeile `  output: buzzer_output` ersetzen durch `  speaker: alarm_speaker`
Zeile `  gain: 70%` ersetzen durch `  gain: 0.8`
(Numerischer Gain-Wert ist korrekt fuer speaker, Prozent-Schreibweise gilt nur fuer output)

Nach diesen Aenderungen soll alarm-clock.yaml enthalten:
- i2s_audio-Block mit korrekten GPIO-Nummern
- speaker-Block mit id: alarm_speaker, dac_type: external
- rtttl-Block mit speaker: alarm_speaker (kein output: mehr)
- output:-Abschnitt enthaelt NUR noch backlight_output
  </action>
  <verify>
    <automated>cd C:/Users/Daniel/dev/esp32-alarm-clock && esphome compile alarm-clock.yaml</automated>
  </verify>
  <acceptance_criteria>
    - alarm-clock.yaml enthaelt `i2s_audio:` als Top-Level-Key
    - alarm-clock.yaml enthaelt `speaker:` als Top-Level-Key mit `id: alarm_speaker`
    - alarm-clock.yaml enthaelt `dac_type: external`
    - alarm-clock.yaml enthaelt `rtttl:` mit `speaker: alarm_speaker` (nicht `output:`)
    - alarm-clock.yaml enthaelt KEINE Zeile `id: buzzer_output`
    - alarm-clock.yaml enthaelt KEINE Zeile `pin: GPIO38` unter einem ledc-output
    - `esphome compile alarm-clock.yaml` endet mit Exit-Code 0 (kein Fehler)
  </acceptance_criteria>
  <done>I2S-Audio-Block konfiguriert, LEDC-Buzzer entfernt, RTTTL referenziert I2S-Speaker, Kompilierung erfolgreich.</done>
</task>

<task type="auto">
  <name>Task 3: GPIO0 Binary-Sensor (Snooze-Button) entfernen</name>
  <files>alarm-clock.yaml</files>
  <read_first>
    - alarm-clock.yaml (vollstaendig lesen um den binary_sensor-Block zu lokalisieren)
  </read_first>
  <action>
Entferne den gesamten binary_sensor-Block fuer snooze_button aus alarm-clock.yaml.

ENTFERNEN -- kompletter binary_sensor-Abschnitt:
```yaml
binary_sensor:
  - platform: gpio
    pin:
      number: GPIO0
      mode:
        input: true
        pullup: true
      inverted: true
      ignore_strapping_warning: true
    id: snooze_button
    name: "Snooze Button"
    internal: true
    on_press:
      - if:
          condition:
            lambda: 'return id(alarm_ringing);'
          then:
            - script.execute: snooze_action
```

Dieser Block faengt mit `binary_sensor:` an und endet nach `- script.execute: snooze_action`.
Entferne ihn vollstaendig (alle ~17 Zeilen). Falls `binary_sensor:` der einzige Top-Level-Key dieser Art ist, entferne auch den Key selbst.

Der Snooze ist weiterhin per Touch auf der alarm_ringing_page moeglich (snooze_btn Button ist unveraendert vorhanden).
  </action>
  <verify>
    <automated>cd C:/Users/Daniel/dev/esp32-alarm-clock && esphome compile alarm-clock.yaml</automated>
  </verify>
  <acceptance_criteria>
    - alarm-clock.yaml enthaelt KEINE Zeile `id: snooze_button`
    - alarm-clock.yaml enthaelt KEINE Zeile `number: GPIO0`
    - alarm-clock.yaml enthaelt KEINE Zeile `ignore_strapping_warning: true`
    - alarm-clock.yaml enthaelt weiterhin `id: snooze_action` (Script bleibt erhalten)
    - `esphome compile alarm-clock.yaml` endet mit Exit-Code 0
  </acceptance_criteria>
  <done>GPIO0 Binary-Sensor vollstaendig entfernt. Snooze-Funktionalitaet nur noch per Touch auf alarm_ringing_page verfuegbar. Kompilierung erfolgreich.</done>
</task>

</tasks>

<verification>
Nach Abschluss aller Tasks:
- `esphome compile alarm-clock.yaml` muss mit Exit-Code 0 enden
- `grep -c "buzzer_output" alarm-clock.yaml` muss 0 zurueckgeben
- `grep -c "snooze_button" alarm-clock.yaml` muss 0 zurueckgeben
- `grep "speaker: alarm_speaker" alarm-clock.yaml` muss einen Treffer liefern
- `grep "i2s_audio:" alarm-clock.yaml` muss einen Treffer liefern
</verification>

<success_criteria>
alarm-clock.yaml kompiliert fehlerfrei mit I2S-Audio-Konfiguration (MAX98357A) statt LEDC-Buzzer. GPIO0-Sensor ist entfernt. RTTTL-Komponente nutzt I2S-Speaker. Die bestehende Alarm-Logik, Scripts und LVGL-Pages sind unveraendert.
</success_criteria>

<output>
Nach Abschluss SUMMARY erstellen: `.planning/phases/02-alarm-kernfunktion/02-01-SUMMARY.md`

Inhalt muss enthalten:
- Welche GPIO-Nummern fuer I2S verwendet werden (BCLK, LRCLK, DOUT)
- Bestaetigung dass Kompilierung erfolgreich ist
- Hinweis auf entfernte Komponenten (buzzer_output, snooze_button)
</output>
