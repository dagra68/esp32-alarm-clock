---
phase: 02-alarm-kernfunktion
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - alarm-clock.yaml
autonomous: true
requirements:
  - ALRM-01
  - ALRM-06
must_haves:
  truths:
    - "alarm-clock.yaml enthaelt Global weekday_only (bool, restore_value: yes)"
    - "alarm-clock.yaml enthaelt Template-Switch 'Nur Mo-Fr' mit id weekday_only_switch"
    - "on_time Lambda nutzt weekday_only Global statt hardcodierter is_weekday-Bedingung"
    - "alarm-clock.yaml kompiliert fehlerfrei nach der Refactoring-Aenderung"
  artifacts:
    - path: "alarm-clock.yaml"
      provides: "weekday_only Global + Switch + refactored on_time Lambda"
      contains: "weekday_only"
  key_links:
    - from: "weekday_only_switch (Template-Switch)"
      to: "weekday_only (Global bool)"
      via: "turn_on_action / turn_off_action mit globals.set"
      pattern: "id: weekday_only_switch"
    - from: "on_time Lambda"
      to: "weekday_only Global"
      via: "!id(weekday_only)"
      pattern: "bool day_ok = !id\\(weekday_only\\)"
---

<objective>
Wochentag-Filter refactoren: Die hardcodierte Mo-Fr-Bedingung in der on_time Lambda durch einen neuen HA-Switch (`weekday_only`) ersetzen. Nutzer kann per Home Assistant zwischen "nur Mo-Fr" und "jeden Tag" umschalten.

Purpose: ALRM-01 erfordert konfigurierbare Wochentag-Einschraenkung. Die aktuelle Hardcodierung macht den Filter nicht aenderbar ohne Firmware-Update.

Output: Neuer `weekday_only` Global (bool, restore_value: yes), neuer Template-Switch "Nur Mo-Fr", refactored on_time Lambda mit dynamischer day_ok-Pruefung.
</objective>

<execution_context>
@C:/Users/Daniel/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/Daniel/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/02-alarm-kernfunktion/02-CONTEXT.md
@.planning/phases/02-alarm-kernfunktion/02-RESEARCH.md

<interfaces>
<!-- Relevante aktuell vorhandene Abschnitte in alarm-clock.yaml -->

Aktueller globals-Block (Zeilen ~159-184):
```yaml
globals:
  - id: alarm_hour
    type: int
    restore_value: yes
    initial_value: '7'
  - id: alarm_minute
    type: int
    restore_value: yes
    initial_value: '0'
  - id: alarm_enabled
    type: bool
    restore_value: yes
    initial_value: 'false'
  - id: alarm_ringing
    type: bool
    restore_value: no
    initial_value: 'false'
  - id: alarm_melody_name
    type: std::string
    restore_value: yes
    max_restore_data_length: 64
    initial_value: '"Reveille"'
  - id: bg_color_hex
    type: int
    restore_value: yes
    initial_value: '0xFF69B4'
```

Aktueller switch-Block (Zeilen ~97-118) -- bestehender alarm_enabled_switch:
```yaml
switch:
  - platform: template
    name: "Alarm aktiv"
    id: alarm_enabled_switch
    icon: mdi:alarm
    restore_mode: DISABLED
    lambda: |-
      return id(alarm_enabled);
    turn_on_action:
      - globals.set:
          id: alarm_enabled
          value: 'true'
      - lvgl.label.update:
          id: analog_alarm_label
          text_color: 0xFFAA00
    turn_off_action:
      - globals.set:
          id: alarm_enabled
          value: 'false'
      - lvgl.label.update:
          id: analog_alarm_label
          text_color: 0x666666
```

Aktuelle hardcodierte on_time Lambda (Zeilen ~319-347) -- WIRD geaendert:
```yaml
on_time:
  - seconds: 0
    minutes: /1
    then:
      - lambda: |-
          if (id(alarm_ringing)) return;
          auto now = id(sntp_time).now();
          if (!now.is_valid()) return;
          bool is_weekday = (now.day_of_week >= 2 && now.day_of_week <= 6);
          if (id(alarm_enabled) && is_weekday &&
              now.hour == id(alarm_hour) && now.minute == id(alarm_minute)) {
            id(alarm_ringing) = true;
            id(alarm_timeout_script).execute();
          }
```

ESPHome day_of_week Referenz:
1=Sonntag, 2=Montag, 3=Dienstag, 4=Mittwoch, 5=Donnerstag, 6=Freitag, 7=Samstag
Mo-Fr: now.day_of_week >= 2 && now.day_of_week <= 6 (bereits korrekt)
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: weekday_only Global ergaenzen</name>
  <files>alarm-clock.yaml</files>
  <read_first>
    - alarm-clock.yaml (vollstaendig lesen um den globals-Block zu lokalisieren)
    - .planning/phases/02-alarm-kernfunktion/02-RESEARCH.md (Pattern 2: weekday_only Global + Switch)
  </read_first>
  <action>
Fuege einen neuen Global-Eintrag am Ende des bestehenden globals:-Blocks in alarm-clock.yaml ein.

**ERGAENZEN -- neuer Global nach dem letzten bestehenden Global (bg_color_hex):**
```yaml
  - id: weekday_only
    type: bool
    restore_value: yes
    initial_value: 'true'
```

Die Einrueckung (2 Leerzeichen) muss mit den anderen List-Items im globals-Block uebereinstimmen.
Der Wert `initial_value: 'true'` bedeutet: Standard ist "nur Mo-Fr aktiv".
  </action>
  <verify>
    <automated>cd C:/Users/Daniel/dev/esp32-alarm-clock && grep -A3 "id: weekday_only" alarm-clock.yaml</automated>
  </verify>
  <acceptance_criteria>
    - alarm-clock.yaml enthaelt `id: weekday_only` im globals-Block
    - alarm-clock.yaml enthaelt `type: bool` fuer diesen Global
    - alarm-clock.yaml enthaelt `restore_value: yes` fuer diesen Global
    - alarm-clock.yaml enthaelt `initial_value: 'true'` fuer diesen Global
  </acceptance_criteria>
  <done>weekday_only Global (bool, restore_value: yes, initial_value: true) im globals-Block vorhanden.</done>
</task>

<task type="auto">
  <name>Task 2: weekday_only Template-Switch ergaenzen</name>
  <files>alarm-clock.yaml</files>
  <read_first>
    - alarm-clock.yaml (vollstaendig lesen um den switch-Block zu lokalisieren)
  </read_first>
  <action>
Fuege einen zweiten Template-Switch in den bestehenden switch:-Block ein. Der switch:-Block enthaelt bereits alarm_enabled_switch -- den neuen weekday_only_switch als weiteres List-Item ergaenzen.

**ERGAENZEN -- neuer Switch nach dem alarm_enabled_switch-Block:**
```yaml
  - platform: template
    name: "Nur Mo-Fr"
    id: weekday_only_switch
    icon: mdi:calendar-week
    restore_mode: RESTORE_DEFAULT_ON
    lambda: |-
      return id(weekday_only);
    turn_on_action:
      - globals.set:
          id: weekday_only
          value: 'true'
    turn_off_action:
      - globals.set:
          id: weekday_only
          value: 'false'
```

`restore_mode: RESTORE_DEFAULT_ON` bedeutet: nach Reboot ist der Switch EIN (nur Mo-Fr aktiv), falls kein gespeicherter Zustand vorliegt.
  </action>
  <verify>
    <automated>cd C:/Users/Daniel/dev/esp32-alarm-clock && grep -A3 "id: weekday_only_switch" alarm-clock.yaml</automated>
  </verify>
  <acceptance_criteria>
    - alarm-clock.yaml enthaelt `id: weekday_only_switch`
    - alarm-clock.yaml enthaelt `name: "Nur Mo-Fr"`
    - alarm-clock.yaml enthaelt `restore_mode: RESTORE_DEFAULT_ON`
    - alarm-clock.yaml enthaelt `icon: mdi:calendar-week`
    - alarm-clock.yaml enthaelt `return id(weekday_only);` in der lambda des weekday_only_switch
  </acceptance_criteria>
  <done>weekday_only_switch Template-Switch vorhanden mit restore_mode RESTORE_DEFAULT_ON und turn_on/off_action die den Global setzen.</done>
</task>

<task type="auto">
  <name>Task 3: on_time Lambda refactoren -- weekday_only statt hardcodiert</name>
  <files>alarm-clock.yaml</files>
  <read_first>
    - alarm-clock.yaml (vollstaendig lesen um die on_time Lambda zu lokalisieren)
    - .planning/phases/02-alarm-kernfunktion/02-RESEARCH.md (Pattern 3: on_time Lambda mit weekday_only-Check)
  </read_first>
  <action>
Ersetze in der on_time Lambda die hardcodierte Wochentag-Bedingung durch den dynamischen weekday_only-Check.

**ERSETZEN -- bestehende Lambda-Zeilen:**
```cpp
          bool is_weekday = (now.day_of_week >= 2 && now.day_of_week <= 6);
          if (id(alarm_enabled) && is_weekday &&
```

**DURCH -- neue Lambda-Zeilen:**
```cpp
          bool day_ok = !id(weekday_only) || (now.day_of_week >= 2 && now.day_of_week <= 6);
          if (id(alarm_enabled) && day_ok &&
```

Logik: `!id(weekday_only)` ist true wenn der Switch AUS ist (Alarm jeden Tag) -- dann ist day_ok immer true. Wenn der Switch EIN ist, gilt die Mo-Fr-Pruefung.

Die restliche Lambda (alarm_ringing check, sntp_time.now(), is_valid(), alarm_timeout_script.execute()) bleibt unveraendert. Ebenso der Rest des on_time-Blocks (rtttl.play, lvgl.page.show, analog_time_update).
  </action>
  <verify>
    <automated>cd C:/Users/Daniel/dev/esp32-alarm-clock && esphome compile alarm-clock.yaml</automated>
  </verify>
  <acceptance_criteria>
    - alarm-clock.yaml enthaelt `bool day_ok = !id(weekday_only) || (now.day_of_week >= 2 && now.day_of_week <= 6);`
    - alarm-clock.yaml enthaelt KEINE Zeile `bool is_weekday = `
    - alarm-clock.yaml enthaelt `if (id(alarm_enabled) && day_ok &&`
    - `esphome compile alarm-clock.yaml` endet mit Exit-Code 0
  </acceptance_criteria>
  <done>on_time Lambda nutzt weekday_only Global. Hardcodierte is_weekday-Bedingung entfernt. Kompilierung erfolgreich.</done>
</task>

</tasks>

<verification>
Nach Abschluss aller Tasks:
- `esphome compile alarm-clock.yaml` muss mit Exit-Code 0 enden
- `grep "id: weekday_only" alarm-clock.yaml` liefert mindestens 2 Treffer (Global + Switch)
- `grep "Nur Mo-Fr" alarm-clock.yaml` liefert 1 Treffer
- `grep "is_weekday" alarm-clock.yaml` liefert 0 Treffer
- `grep "day_ok" alarm-clock.yaml` liefert mindestens 1 Treffer
</verification>

<success_criteria>
alarm-clock.yaml kompiliert fehlerfrei mit weekday_only Global und Template-Switch. Die on_time Lambda nutzt den dynamischen day_ok-Check. Nutzer kann per Home Assistant zwischen "nur Mo-Fr" und "jeden Tag" umschalten.
</success_criteria>

<output>
Nach Abschluss SUMMARY erstellen: `.planning/phases/02-alarm-kernfunktion/02-02-SUMMARY.md`

Inhalt muss enthalten:
- Bestaetigung dass Kompilierung erfolgreich ist
- Welche Aenderungen gemacht wurden (3 Tasks)
- Hinweis: weekday_only_switch erscheint in HA als Switch "Nur Mo-Fr"
</output>
